# CLAUDE.md

Read `AGENTS.md` first.
Then read `README.md`, `docs/manuals/README.md`, and the relevant workspace `PATTERNS.md` / `CHANGELOG.md`.

Follow `AGENTS.md`, keep diffs small, prefer test-first for behavior changes, and update matching workspace docs when code or behavior changes.
